import JobCard from "./JobCard";
import Filter from '../../filter/components/Filter'

const Finder = () => {
  return (
    <div>
      Finder
      <Filter/>
      <JobCard />
    </div>
  );
};

export default Finder;
